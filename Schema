create table student(

id int NOT NULL AUTO_INCREMENT,
name VARCHAR(255) UNIQUE NOT NULL,
last_name VARCHAR(255),
classes JSON,
subjects JSON,
request_response INT,
password varchar(255),
role varchar(255),
PRIMARY KEY (id)
);


